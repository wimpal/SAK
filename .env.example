# Optional Spotify Developer credentials for large playlists (>100 tracks).
# Copy to `.env` in this folder (repo root) and fill in your app values.
# Never commit `.env` — it is gitignored.

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
